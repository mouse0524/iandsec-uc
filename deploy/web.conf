log_format waf_debug '$remote_addr xff="$http_x_forwarded_for" cf="$http_cf_connecting_ip" real="$http_x_real_ip" true="$http_true_client_ip" "$request"';

server {
        listen 8080;
        server_name localhost; 
        client_max_body_size 50m;
        access_log /var/log/nginx/access.log waf_debug;
        include /etc/nginx/snippets/security-headers.conf;
        location = /index.html {
              root /opt/iandsec-uc/web/dist;
              include /etc/nginx/snippets/security-headers.conf;
              add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate" always;
              add_header Pragma "no-cache" always;
              add_header Expires "0" always;
        }
        location / {
              root /opt/iandsec-uc/web/dist;
              index index.html index.htm;
              try_files $uri /index.html;
              include /etc/nginx/snippets/security-headers.conf;
              add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate" always;
              add_header Pragma "no-cache" always;
              add_header Expires "0" always;
        }
        location ^~ /assets/ {
              root /opt/iandsec-uc/web/dist;
              try_files $uri =404;
              error_page 404 =200 /asset-missing.js;
              include /etc/nginx/snippets/security-headers.conf;
              add_header Cache-Control "public, max-age=31536000, immutable" always;
        }
        location ^~ /api/ {
                proxy_set_header Host $host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header X-Forwarded-Proto $scheme;
                proxy_pass http://127.0.0.1:9999;
        }

}
